import { Request, Response, NextFunction } from 'express';

export const {{.MiddlewareName}}Middleware = (
  _req: Request,
  _res: Response,
  next: NextFunction
): void => {
  // TODO: Implement {{.MiddlewareName}} logic
  next();
};
